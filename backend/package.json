{
  "name": "testproject-backend",
  "version": "1.0.0",
  "description": "Lambda handlers (CRUD de veiculos) para o testproject - modelo compartilhado (stage -> tabela DynamoDB)",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "jest --runInBand",
    "lint": "node --check src/handlers/getVeiculos.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^4.0.1",
    "jest": "^29.7.0"
  }
}
